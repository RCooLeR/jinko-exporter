# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

project_name: jinko-exporter

before:
  hooks:
    - go -C bridge mod download

builds:
  - id: jinko-exporter
    main: .
    dir: bridge
    binary: jinko-exporter
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/RCooLeR/jinko-exporter/bridge/internal/buildinfo.Version={{ .Version }} -X github.com/RCooLeR/jinko-exporter/bridge/internal/buildinfo.Commit={{ .FullCommit }} -X github.com/RCooLeR/jinko-exporter/bridge/internal/buildinfo.Date={{ .Date }}

archives:
  - id: jinko-exporter
    ids:
      - jinko-exporter
    formats:
      - tar.gz
    files:
      - README.md
      - LICENSE
      - NOTICE
      - compose.example.yaml
      - bridge/docs/*.md
      - ha-cards/README.md
      - ha-cards/docs/*.md
      - ha-cards/dist/jinko-ha-cards.js
      - new-ha-cards/README.md
      - new-ha-cards/dist/jinko-ha-cards.js

checksum:
  name_template: checksums.txt

release:
  preflight:
    fail_on_error: true

dockers_v2:
  - id: dockerhub
    ids:
      - jinko-exporter
    dockerfile: bridge/Dockerfile.goreleaser
    images:
      - rcooler/jinko_exporter
    tags:
      - "{{ .Version }}"
      - "{{ if not .Prerelease }}{{ .Major }}.{{ .Minor }}{{ end }}"
      - "{{ if not .Prerelease }}{{ .Major }}{{ end }}"
      - "{{ if not .Prerelease }}latest{{ end }}"
    labels:
      org.opencontainers.image.title: jinko-exporter
      org.opencontainers.image.description: Read-only Prometheus and Home Assistant bridge for Jinko, Solarman, and locked local Modbus telemetry
      org.opencontainers.image.created: "{{ .Date }}"
      org.opencontainers.image.documentation: "https://github.com/RCooLeR/jinko-exporter"
      org.opencontainers.image.licenses: MIT
      org.opencontainers.image.source: "{{ .GitURL }}"
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"
    sbom: true
    platforms:
      - linux/amd64
      - linux/arm64

docker_digest:
  name_template: digests.txt

changelog:
  sort: asc
